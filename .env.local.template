MODEL_PATH=~/models/Llama-3.1-8B-Instruct
DEVICE=mps
CLUSTER=local

# vLLM is not supported on macOS/MPS.
# For local LLM testing, either:
#   a) Run --baseline-only (no LLM needed)
#   b) Start a local vLLM server manually on a CUDA machine and set:
#        VLLM_BASE_URL=http://<host>:8000/v1
# Without VLLM_BASE_URL, run_experiment.py falls back to LLMHuggingFace (MPS).
